diff --git a/libhrx/cmake/hrx_exports_macos.txt b/libhrx/cmake/hrx_exports_macos.txt
index 95e8bbf2..a5e6218f 100644
--- a/libhrx/cmake/hrx_exports_macos.txt
+++ b/libhrx/cmake/hrx_exports_macos.txt
@@ -39,6 +39,8 @@ _hrx_semaphore_query
 _hrx_semaphore_wait
 _hrx_semaphore_signal
 _hrx_stream_create
+_hrx_stream_create_on_queue
+_hrx_stream_get_queue_affinity
 _hrx_stream_retain
 _hrx_stream_release
 _hrx_stream_synchronize
diff --git a/libhrx/src/libhrx/buffer.c b/libhrx/src/libhrx/buffer.c
index a8848b0d..2c52bfe7 100644
--- a/libhrx/src/libhrx/buffer.c
+++ b/libhrx/src/libhrx/buffer.c
@@ -78,8 +78,16 @@ hrx_status_t hrx_buffer_allocate(hrx_stream_t stream, size_t size,
   };

   iree_status_t status = iree_ok_status();
+#if !defined(IREE_HAL_AMDGPU_MACOS_COARSE_HOST_ADAPTER)
   status = hrx_iree_exact_pool_create(stream->device->allocator.hal_allocator,
                                       params, &buf->hal_pool);
+#endif
+  // The macOS adapter uses the device's existing TLSF/oversized pool set.
+  // A NULL explicit pool selects that set. The transient buffer owns its
+  // reservation; the HRX buffer retains the device that owns the default pools,
+  // and hrx_buffer_release releases the buffer before dropping that device.
+  // An exact pool per tensor would consume a driver BO for every small state.
+
   if (iree_status_is_ok(status)) {
     status = iree_hal_device_queue_alloca(
         stream->device->hal_device,
diff --git a/libhrx/src/libhrx/runtime.c b/libhrx/src/libhrx/runtime.c
index 396c75cf..8afad494 100644
--- a/libhrx/src/libhrx/runtime.c
+++ b/libhrx/src/libhrx/runtime.c
@@ -491,7 +491,11 @@ static iree_status_t hrx_get_profile_data_families(
     *out_data_families = IREE_HAL_DEVICE_PROFILING_DATA_QUEUE_EVENTS;
   } else if (strcmp(value, "dispatch") == 0) {
     *out_data_families = IREE_HAL_DEVICE_PROFILING_DATA_EXECUTABLE_METADATA |
+#if defined(IREE_HAL_AMDGPU_MACOS_COARSE_HOST_ADAPTER)
+                         IREE_HAL_DEVICE_PROFILING_DATA_QUEUE_EVENTS |
+#else
                          IREE_HAL_DEVICE_PROFILING_DATA_DEVICE_QUEUE_EVENTS |
+#endif
                          IREE_HAL_DEVICE_PROFILING_DATA_DISPATCH_EVENTS;
   } else if (strcmp(value, "executable") == 0) {
     *out_data_families = IREE_HAL_DEVICE_PROFILING_DATA_EXECUTABLE_METADATA |
diff --git a/libhrx/src/libhrx/stream.c b/libhrx/src/libhrx/stream.c
index 7d5319af..91abe2e4 100644
--- a/libhrx/src/libhrx/stream.c
+++ b/libhrx/src/libhrx/stream.c
@@ -6,6 +6,10 @@
 // Adapted from iree-hal-streaming's stream.c timeline semaphore pattern.

 #include <stdlib.h>
+#if defined(IREE_HAL_AMDGPU_MACOS_COARSE_HOST_ADAPTER)
+#include <pthread.h>
+static pthread_mutex_t hrx_profile_flush_mutex = PTHREAD_MUTEX_INITIALIZER;
+#endif

 #include "hrx_internal.h"

@@ -198,7 +202,20 @@ hrx_status_t hrx_stream_synchronize(hrx_stream_t stream) {
   hrx_status_t status = hrx_stream_flush(stream);
   if (!hrx_status_is_ok(status)) HRX_RETURN_AND_END_ZONE(z0, status);

-  HRX_RETURN_AND_END_ZONE(z0, hrx_stream_wait(stream));
+  status = hrx_stream_wait(stream);
+#if defined(IREE_HAL_AMDGPU_MACOS_COARSE_HOST_ADAPTER)
+  if (hrx_status_is_ok(status) && stream->device->profiling_active) {
+    // Retire ready events at the application's existing synchronization point.
+    // No extra device wait and no per-dispatch flush: this bounds the 64K event
+    // ring across long model runs while preserving normal batching.
+    pthread_mutex_lock(&hrx_profile_flush_mutex);
+    iree_status_t flushed =
+        iree_hal_device_profiling_flush(stream->device->hal_device);
+    pthread_mutex_unlock(&hrx_profile_flush_mutex);
+    status = hrx_status_from_iree(flushed);
+  }
+#endif
+  HRX_RETURN_AND_END_ZONE(z0, status);
 }

 hrx_status_t hrx_stream_wait(hrx_stream_t stream) {
diff --git a/runtime/src/iree/hal/drivers/amdgpu/access_policy.c b/runtime/src/iree/hal/drivers/amdgpu/access_policy.c
index 48b80aa0..605ae8ee 100644
--- a/runtime/src/iree/hal/drivers/amdgpu/access_policy.c
+++ b/runtime/src/iree/hal/drivers/amdgpu/access_policy.c
@@ -85,6 +85,39 @@ iree_status_t iree_hal_amdgpu_access_allow_agent_list(
   IREE_ASSERT_ARGUMENT(libhsa);
   IREE_ASSERT_ARGUMENT(agent_list);
   IREE_ASSERT_ARGUMENT(ptr);
+#if defined(IREE_HAL_AMDGPU_MACOS_COARSE_HOST_ADAPTER)
+  // The topology list includes the nearest CPU even for device-only slabs.
+  // This transport's VRAM pointers have no CPU mapping. Do not ask HSA to
+  // grant a mapping it cannot provide; mapped host/GTT allocations retain
+  // their full list, and unsupported peer-GPU requests must still fail.
+  hsa_amd_pointer_info_t info = {0};
+  info.size = sizeof(info);
+  IREE_RETURN_IF_ERROR(iree_hsa_amd_pointer_info(
+      IREE_LIBHSA(libhsa), ptr, &info, NULL, NULL, NULL));
+  iree_hal_amdgpu_access_agent_list_t filtered = {0};
+  if (info.type == HSA_EXT_POINTER_TYPE_HSA && !info.hostBaseAddress) {
+    hsa_device_type_t owner_type = HSA_DEVICE_TYPE_CPU;
+    IREE_RETURN_IF_ERROR(iree_hsa_agent_get_info(
+        IREE_LIBHSA(libhsa), info.agentOwner, HSA_AGENT_INFO_DEVICE,
+        &owner_type));
+    if (owner_type == HSA_DEVICE_TYPE_GPU) {
+      for (uint32_t i = 0; i < agent_list->count; ++i) {
+        hsa_device_type_t type = HSA_DEVICE_TYPE_CPU;
+        IREE_RETURN_IF_ERROR(iree_hsa_agent_get_info(
+            IREE_LIBHSA(libhsa), agent_list->values[i], HSA_AGENT_INFO_DEVICE,
+            &type));
+        if (type != HSA_DEVICE_TYPE_CPU) {
+          filtered.values[filtered.count++] = agent_list->values[i];
+        }
+      }
+      if (!filtered.count) {
+        return iree_make_status(IREE_STATUS_INVALID_ARGUMENT,
+                                "device-only VRAM has no requested GPU agent");
+      }
+      agent_list = &filtered;
+    }
+  }
+#endif
   return iree_hsa_amd_agents_allow_access(IREE_LIBHSA(libhsa),
                                           agent_list->count, agent_list->values,
                                           /*flags=*/NULL, ptr);
diff --git a/runtime/src/iree/hal/drivers/amdgpu/host_queue.c b/runtime/src/iree/hal/drivers/amdgpu/host_queue.c
index 08590310..43343040 100644
--- a/runtime/src/iree/hal/drivers/amdgpu/host_queue.c
+++ b/runtime/src/iree/hal/drivers/amdgpu/host_queue.c
@@ -7,6 +7,7 @@
 #include "iree/hal/drivers/amdgpu/host_queue.h"

 #include <string.h>
+#include <stdlib.h>

 #include "iree/async/frontier_tracker.h"
 #include "iree/async/notification.h"
@@ -895,6 +896,19 @@ iree_status_t iree_hal_amdgpu_host_queue_initialize(
   // Initialize the AQL ring from the hardware queue.
   if (iree_status_is_ok(status)) {
     out_queue->hardware_queue = hardware_queue;
+#if defined(IREE_HAL_AMDGPU_MACOS_COARSE_HOST_ADAPTER)
+    // Opt-in process capture must latch CP profiling before any setup dispatch.
+    // Starting a new collection on an already used queue is intentionally not
+    // supported by this adapter; it would require synchronized queue remapping.
+    const char* profile_file = getenv("HRX_PROFILE_FILE");
+    const char* profile_mode = getenv("HRX_PROFILE_MODE");
+    if (profile_file && profile_file[0] && profile_mode &&
+        strcmp(profile_mode, "dispatch") == 0) {
+      status = iree_hsa_amd_profiling_set_profiler_enabled(
+          IREE_LIBHSA(libhsa), hardware_queue, 1);
+      out_queue->profiling.macos_profile_latched = iree_status_is_ok(status);
+    }
+#endif
     iree_hal_amdgpu_aql_ring_initialize(
         libhsa, (iree_amd_queue_t*)hardware_queue, &out_queue->aql_ring);
   }
@@ -1098,6 +1112,13 @@ iree_status_t iree_hal_amdgpu_host_queue_set_hsa_profiling_enabled(
   IREE_TRACE_ZONE_BEGIN(z0);
   IREE_TRACE_ZONE_APPEND_VALUE_I64(z0, enabled ? 1 : 0);

+#if defined(IREE_HAL_AMDGPU_MACOS_COARSE_HOST_ADAPTER)
+  if (enabled && !queue->profiling.macos_profile_latched) {
+    return iree_make_status(IREE_STATUS_FAILED_PRECONDITION,
+        "macOS dispatch profiling requires HRX_PROFILE_FILE and "
+        "HRX_PROFILE_MODE=dispatch before device creation");
+  }
+#endif
   if (enabled) {
     IREE_RETURN_AND_END_ZONE_IF_ERROR(
         z0, iree_hal_amdgpu_host_queue_ensure_profile_event_storage(queue));
@@ -1107,8 +1128,14 @@ iree_status_t iree_hal_amdgpu_host_queue_set_hsa_profiling_enabled(
     iree_hal_amdgpu_host_queue_clear_profile_events(queue);
   }

+#if defined(IREE_HAL_AMDGPU_MACOS_COARSE_HOST_ADAPTER)
+  // Collection can end while the immutable CP property remains enabled. Its
+  // queue is retired normally; future unprofiled packets have no private slots.
+  iree_status_t status = iree_ok_status();
+#else
   iree_status_t status = iree_hsa_amd_profiling_set_profiler_enabled(
       IREE_LIBHSA(queue->libhsa), queue->hardware_queue, enabled ? 1 : 0);
+#endif
   if (iree_status_is_ok(status)) {
     queue->profiling.hsa_queue_timestamps_enabled = enabled ? 1 : 0;
   }
diff --git a/runtime/src/iree/hal/drivers/amdgpu/host_queue.h b/runtime/src/iree/hal/drivers/amdgpu/host_queue.h
index c2f43b06..5e2f6062 100644
--- a/runtime/src/iree/hal/drivers/amdgpu/host_queue.h
+++ b/runtime/src/iree/hal/drivers/amdgpu/host_queue.h
@@ -345,6 +345,9 @@ typedef struct iree_hal_amdgpu_host_queue_t {
   struct {
     // True when ROCR should populate dispatch completion signal timestamps.
     uint32_t hsa_queue_timestamps_enabled : 1;
+    // macOS: CP property latched before the first packet, until queue retirement.
+    uint32_t macos_profile_latched : 1;
+    uint32_t completion_signals_ready : 1;
     // True when host-side queue operation events should be recorded.
     uint32_t queue_events_enabled : 1;
     // True when device-timestamped queue operation events should be recorded.
diff --git a/runtime/src/iree/hal/drivers/amdgpu/host_queue_profile_events.c b/runtime/src/iree/hal/drivers/amdgpu/host_queue_profile_events.c
index 210111d4..2b6dead3 100644
--- a/runtime/src/iree/hal/drivers/amdgpu/host_queue_profile_events.c
+++ b/runtime/src/iree/hal/drivers/amdgpu/host_queue_profile_events.c
@@ -142,12 +142,16 @@ iree_hal_amdgpu_host_queue_allocate_profiling_completion_signals(
         (iree_host_size_t)iree_alignof(iree_amd_signal_t));
   }
   if (iree_status_is_ok(status)) {
+    // Publish ownership before a setup dispatch can reference this allocation.
+    // A failed/timeout initialization is retired by normal queue teardown; it
+    // does not prove the GPU has stopped using the signal storage.
+    queue->profiling.completion_signals = signals;
     status = iree_hal_amdgpu_host_queue_initialize_profiling_completion_signals(
         queue, signals, signal_count);
+    queue->profiling.completion_signals_ready = iree_status_is_ok(status);
   }
-  if (iree_status_is_ok(status)) {
-    queue->profiling.completion_signals = signals;
-  } else if (signals) {
+  if (!iree_status_is_ok(status) && signals &&
+      !queue->profiling.completion_signals) {
     status = iree_status_join(status, iree_hsa_amd_memory_pool_free(
                                           IREE_LIBHSA(queue->libhsa), signals));
   }
@@ -158,7 +162,11 @@ iree_hal_amdgpu_host_queue_allocate_profiling_completion_signals(

 iree_status_t iree_hal_amdgpu_host_queue_ensure_profiling_completion_signals(
     iree_hal_amdgpu_host_queue_t* queue) {
-  if (queue->profiling.completion_signals) return iree_ok_status();
+  if (queue->profiling.completion_signals) {
+    return queue->profiling.completion_signals_ready ? iree_ok_status() :
+        iree_make_status(IREE_STATUS_FAILED_PRECONDITION,
+                        "profiling signal initialization previously failed");
+  }
   IREE_RETURN_IF_ERROR(
       iree_hal_amdgpu_host_queue_require_profiling_signal_memory_pool(queue));
   return iree_hal_amdgpu_host_queue_allocate_profiling_completion_signals(
@@ -173,6 +181,7 @@ void iree_hal_amdgpu_host_queue_deallocate_profiling_completion_signals(
   iree_hal_amdgpu_hsa_cleanup_assert_success(iree_hsa_amd_memory_pool_free_raw(
       queue->libhsa, queue->profiling.completion_signals));
   queue->profiling.completion_signals = NULL;
+  queue->profiling.completion_signals_ready = 0;

   IREE_TRACE_ZONE_END(z0);
 }
diff --git a/runtime/src/iree/hal/drivers/amdgpu/logical_device.c b/runtime/src/iree/hal/drivers/amdgpu/logical_device.c
index be7c4cfc..67dcfbe8 100644
--- a/runtime/src/iree/hal/drivers/amdgpu/logical_device.c
+++ b/runtime/src/iree/hal/drivers/amdgpu/logical_device.c
@@ -676,6 +676,12 @@ static bool iree_hal_amdgpu_logical_device_profiling_needs_hsa_timestamps(

 static bool iree_hal_amdgpu_logical_device_profiling_needs_clock_correlations(
     iree_hal_device_profiling_data_families_t data_families) {
+#if defined(IREE_HAL_AMDGPU_MACOS_COARSE_HOST_ADAPTER)
+  // Raw CP ticks are meaningful within the GPU clock domain. Do not fabricate
+  // host/GPU samples when the runtime has no calibrated clock-counter API.
+  (void)data_families;
+  return false;
+#endif
   return iree_hal_amdgpu_logical_device_profiling_needs_hsa_timestamps(
              data_families) ||
          iree_any_bit_set(data_families,
@@ -2021,6 +2027,36 @@ iree_status_t iree_hal_amdgpu_logical_device_create(
   iree_hal_amdgpu_logical_device_options_t resolved_options = *options;
   iree_hal_amdgpu_logical_device_options_apply_create_params(&resolved_options,
                                                              create_params);
+#if defined(IREE_HAL_AMDGPU_MACOS_COARSE_HOST_ADAPTER)
+  // The macOS transport currently provides one GPU domain and CPU-published
+  // AQL queues. Reject features requiring GPU VA aliases or mixed CPU/GPU RMW.
+  if (topology->gpu_agent_count != 1 || hostcall_provider ||
+      resolved_options.feedback.enabled || resolved_options.asan.enabled ||
+      resolved_options.tsan.enabled ||
+      resolved_options.queue_placement == IREE_HAL_AMDGPU_QUEUE_PLACEMENT_DEVICE ||
+      resolved_options.command_buffer_mode == IREE_HAL_AMDGPU_COMMAND_BUFFER_MODE_PM4) {
+    IREE_RETURN_AND_END_ZONE_IF_ERROR(z0, iree_make_status(
+        IREE_STATUS_UNIMPLEMENTED,
+        "macOS coarse-host adapter supports one GPU, host AQL queues, and "
+        "uninstrumented dispatch; hostcalls, device scheduling, PM4 replay, "
+        "and ASAN/TSAN/feedback require additional transport contracts"));
+  }
+  uint32_t queue_size_max = 0;
+  IREE_RETURN_AND_END_ZONE_IF_ERROR(z0, iree_hsa_agent_get_info(
+      IREE_LIBHSA(libhsa), topology->gpu_agents[0],
+      HSA_AGENT_INFO_QUEUE_MAX_SIZE, &queue_size_max));
+  if (queue_size_max < 64 || !iree_host_size_is_power_of_two(queue_size_max)) {
+    IREE_RETURN_AND_END_ZONE_IF_ERROR(z0, iree_make_status(
+        IREE_STATUS_FAILED_PRECONDITION, "invalid macOS HSA queue capacity"));
+  }
+  if (resolved_options.host_queues.aql_capacity > queue_size_max) {
+    resolved_options.host_queues.aql_capacity = queue_size_max;
+  }
+  resolved_options.command_buffer_mode = IREE_HAL_AMDGPU_COMMAND_BUFFER_MODE_AQL;
+  // Timeline dependencies are resolved by the host until vendor PM4 packets
+  // are separately validated on this transport.
+  resolved_options.force_wait_barrier_defer = true;
+#endif
   // PM4 and automatic modes promise reusable dispatch command buffers,
   // including dynamic binding-table fixups. Provision their queue-control
   // storage here so a valid recording cannot fail only when first submitted.
@@ -2792,6 +2828,15 @@ static iree_status_t iree_hal_amdgpu_logical_device_create_command_buffer(
     iree_hal_command_buffer_t** out_command_buffer) {
   iree_hal_amdgpu_logical_device_t* logical_device =
       iree_hal_amdgpu_logical_device_cast(base_device);
+#if defined(IREE_HAL_AMDGPU_MACOS_COARSE_HOST_ADAPTER)
+  // HRX streams do not request retained profile metadata themselves. A capture
+  // must retain command IDs and dispatch summaries at recording time; without
+  // them the upstream collector deliberately skips every dispatch.
+  if (iree_hal_amdgpu_logical_device_profiling_requests_dispatch_timestamps(
+          logical_device)) {
+    mode |= IREE_HAL_COMMAND_BUFFER_MODE_RETAIN_PROFILE_METADATA;
+  }
+#endif
   iree_hal_queue_affinity_t effective_queue_affinity = 0;
   iree_host_size_t device_ordinal = 0;
   IREE_RETURN_IF_ERROR(
@@ -3199,6 +3244,16 @@ iree_hal_amdgpu_logical_device_verify_queue_device_profiling_supported(
 static iree_status_t iree_hal_amdgpu_logical_device_profiling_begin(
     iree_hal_device_t* base_device,
     const iree_hal_device_profiling_options_t* options) {
+#if defined(IREE_HAL_AMDGPU_MACOS_COARSE_HOST_ADAPTER)
+  const uint64_t supported = IREE_HAL_DEVICE_PROFILING_DATA_QUEUE_EVENTS |
+      IREE_HAL_DEVICE_PROFILING_DATA_EXECUTABLE_METADATA |
+      IREE_HAL_DEVICE_PROFILING_DATA_DISPATCH_EVENTS;
+  if (!options || (options->data_families & ~supported)) {
+    return iree_make_status(IREE_STATUS_UNIMPLEMENTED,
+        "macOS profiling supports host queue events, executable metadata and "
+        "CP dispatch timestamps only; counters/PM4/clock correlation unavailable");
+  }
+#endif
   iree_hal_amdgpu_logical_device_t* logical_device =
       iree_hal_amdgpu_logical_device_cast(base_device);
   iree_hal_device_profiling_options_t resolved_options =
diff --git a/runtime/src/iree/hal/drivers/amdgpu/physical_device.c b/runtime/src/iree/hal/drivers/amdgpu/physical_device.c
index 40374788..60007355 100644
--- a/runtime/src/iree/hal/drivers/amdgpu/physical_device.c
+++ b/runtime/src/iree/hal/drivers/amdgpu/physical_device.c
@@ -903,6 +903,13 @@ iree_hal_amdgpu_physical_device_initialize_vendor_packet_strategy(
     wait_barrier_strategy = iree_hal_amdgpu_select_wait_barrier_strategy(
         vendor_packet_capabilities);
   }
+#if defined(IREE_HAL_AMDGPU_MACOS_COARSE_HOST_ADAPTER)
+  // No vendor packets have been validated by this adapter. This also prevents
+  // queue setup from requesting an executable PM4 IB pool allocation.
+  vendor_packet_capabilities = 0;
+  pm4_timestamp_strategy = IREE_HAL_AMDGPU_PM4_TIMESTAMP_STRATEGY_NONE;
+  wait_barrier_strategy = IREE_HAL_AMDGPU_WAIT_BARRIER_STRATEGY_DEFER;
+#endif
   out_physical_device->agent_target = agent_target;
   out_physical_device->vendor_packet_capabilities = vendor_packet_capabilities;
   out_physical_device->wait_barrier_strategy = wait_barrier_strategy;
diff --git a/runtime/src/iree/hal/drivers/amdgpu/system.c b/runtime/src/iree/hal/drivers/amdgpu/system.c
index 12d8462e..e776e6c9 100644
--- a/runtime/src/iree/hal/drivers/amdgpu/system.c
+++ b/runtime/src/iree/hal/drivers/amdgpu/system.c
@@ -115,6 +115,16 @@ static iree_status_t iree_hal_amdgpu_system_populate_host_memory_pools(
                              "no accessible-by-all + coarse-grained shared "
                              "memory pool is available in the system"));
   }
+#if defined(IREE_HAL_AMDGPU_MACOS_COARSE_HOST_ADAPTER)
+  // This adapter uses dispatch publication and completion for ownership
+  // transfer. It does not change the HSA pool flags or promise CPU/GPU RMW
+  // interoperability. Logical-device creation rejects protocols requiring it.
+  if (!host_memory_pools->fine_pool.handle &&
+      host_memory_pools->coarse_pool.handle ==
+          host_memory_pools->kernarg_pool.handle) {
+    host_memory_pools->fine_pool = host_memory_pools->coarse_pool;
+  }
+#endif
   if (!host_memory_pools->fine_pool.handle) {
     IREE_RETURN_AND_END_ZONE_IF_ERROR(
         z0, iree_make_status(IREE_STATUS_NOT_FOUND,
diff --git a/runtime/src/iree/hal/drivers/amdgpu/util/info.c b/runtime/src/iree/hal/drivers/amdgpu/util/info.c
index fae2b3ee..c9958e0b 100644
--- a/runtime/src/iree/hal/drivers/amdgpu/util/info.c
+++ b/runtime/src/iree/hal/drivers/amdgpu/util/info.c
@@ -68,13 +68,15 @@ iree_status_t iree_hal_amdgpu_system_info_query(
                                HSA_AMD_SYSTEM_INFO_SVM_SUPPORTED,
                                &svm_supported),
       "querying HSA_AMD_SYSTEM_INFO_SVM_SUPPORTED");
+#if !defined(IREE_HAL_AMDGPU_MACOS_COARSE_HOST_ADAPTER)
   if (!svm_supported) {
     IREE_RETURN_AND_END_ZONE_IF_ERROR(
         z0, iree_make_status(IREE_STATUS_INCOMPATIBLE,
                              "only systems with SVM are supported "
                              "(HSA_AMD_SYSTEM_INFO_SVM_SUPPORTED == true)"));
   }
-  out_info->svm.supported = 1;
+#endif
+  out_info->svm.supported = svm_supported ? 1 : 0;

   bool svm_accessible_by_default = false;
   IREE_RETURN_AND_END_ZONE_IF_ERROR(
@@ -110,6 +112,7 @@ iree_status_t iree_hal_amdgpu_system_info_query(
                                HSA_AMD_SYSTEM_INFO_VIRTUAL_MEM_API_SUPPORTED,
                                &virtual_mem_api_supported),
       "querying HSA_AMD_SYSTEM_INFO_VIRTUAL_MEM_API_SUPPORTED");
+#if !defined(IREE_HAL_AMDGPU_MACOS_COARSE_HOST_ADAPTER)
   if (!virtual_mem_api_supported) {
     IREE_RETURN_AND_END_ZONE_IF_ERROR(
         z0, iree_make_status(
@@ -118,6 +121,8 @@ iree_status_t iree_hal_amdgpu_system_info_query(
                 "(HSA_AMD_SYSTEM_INFO_VIRTUAL_MEM_API_SUPPORTED == true)"));
   }

+#endif
+
   IREE_RETURN_AND_END_ZONE_IF_ERROR(
       z0,
       iree_hsa_system_get_info(IREE_LIBHSA(libhsa),
diff --git a/runtime/src/iree/hal/drivers/amdgpu/util/libhsa.c b/runtime/src/iree/hal/drivers/amdgpu/util/libhsa.c
index 18ff2256..ab1d3837 100644
--- a/runtime/src/iree/hal/drivers/amdgpu/util/libhsa.c
+++ b/runtime/src/iree/hal/drivers/amdgpu/util/libhsa.c
@@ -282,6 +282,9 @@ static const char* iree_hal_amdgpu_libhsa_names[] = {
     // appears on Windows again (🤞) we'll update this to point at it. For now
     // users can still build the HAL driver but it won't run.
     "hsa-runtime64.dll",
+#elif defined(IREE_PLATFORM_APPLE)
+    "libhsa-runtime64.1.dylib",
+    "libhsa-runtime64.dylib",
 #else
     // Versioned soname first — this is the real soname baked into the library
     // and is always present. The unversioned .so is a development symlink that
